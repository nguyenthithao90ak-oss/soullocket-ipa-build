import 'package:flutter/material.dart';

class HomeInteractionController extends ChangeNotifier {
  // Add interaction state here (e.g., flying hearts, poke)
  // For now, this is a placeholder that we will build out.
}
