Sticker intake cho app.

Đặt file theo 2 bước:

1. `sheets/`
   Dùng cho ảnh gốc chưa cắt, ví dụ sticker sheet tải từ ngoài về.

2. `cutout/`
   Dùng cho từng sticker đã tách riêng, ưu tiên `png` hoặc `webp`, nền trong suốt.

Khi bạn đã chép file vào đây, chỉ cần nhắn đúng path để mình xử lý tiếp.
